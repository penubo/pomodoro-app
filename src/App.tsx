import React from 'react';
import TimerPage from './TimerPage';

interface AppProps {}

function App({}: AppProps) {
  return (
    <div className="App">
      <TimerPage />
    </div>
  );
}

export default App;
